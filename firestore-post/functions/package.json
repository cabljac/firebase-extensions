{
  "name": "firestore-post",
  "description": "Make a POST request to a URL with the data from a Firestore document.",
  "author": "Jacob Cable",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "main": "lib/index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "npm run clean && npm run compile",
    "clean": "rimraf lib",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-readme": "firebase ext:info .. --markdown > ../README.md",
    "format": "prettier --write \"**/*.{js,md,yml,ts,json,yaml}\"",
    "lint": "prettier --check \"**/*.{js,md,yml,ts,json,yaml}\""
  },
  "dependencies": {
    "@types/express-serve-static-core": "4.17.30",
    "@types/node": "14.18.34",
    "axios": "^0.19.1",
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.0",
    "jest": "^29.4.2",
    "mustache": "^4.2.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  },
  "private": true,
  "devDependencies": {
    "@types/mustache": "^4.2.2",
    "dotenv": "^16.0.3",
    "eslint": "^8.34.0",
    "prettier": "^2.8.4"
  }
}